# Tracker

Live record of tasks, decisions, and progress. Updated at each stage.

## Status

| Stage | Task | Status |
|------|------|--------|
| 0 | Move rules/design/game-logic into `rules/` | Done |
| 0 | Create `rules/plan.md` + `rules/tracker.md` | Done |
| 0 | git init + .gitignore | Done |
| 0 | Scaffold Next.js 16 + React 19 + TS + Tailwind v4 | Done |
| 1 | Design token + theme system | Done |
| 2 | Headless game engine + tests | Done |
| 3 | Classic mode + UI shell | Done |
| 4 | Sound + haptics | Done (folded into shell) |
| 5 | Zen / Rush / Daily / No Guess | Done |
| 6 | Settings + persistence (stats, achievements, records) | Done |
| 7 | Custom primitives + polish | Done |
| 8 | PWA, meta, docs | Pending |

## Decisions

- **2026-09-13** Stack confirmed: Next.js 16 + React 19 + TS strict + Tailwind v4 with CSS-variable-driven tokens. User approved option 1 and asked Tailwind decision to be made after reviewing the reference folder. Decision: use Tailwind v4 as engine, all configurable values are runtime custom properties.
- **2026-09-13** Design reference: `D:\Programming\html-css\loksewa quiz hosted`. Token strategy mirrors its `[data-theme]` approval, iOS HIG material, frosted glass, safe areas, spring motion tokens.
- **2026-09-13** Project name: SweeperMine. Tagline: "Think clearly. Clear everything."
- **2026-09-13** No backend. Fully local. PWA offline-first. V1 modes: Classic (Beginner/Intermediate/Expert/Custom), Zen, Rush, Daily, No Guess.
- **2026-09-13** Engine architecture finalized: `src/engine/` (pure TS, zero React) with modular `rng` (mulberry32 + hashed seeds), `topology` (Square/Cylinder + adjacency), `actions` (flood/chord/flag mutate cells only), `GameEngine` state machine (phase, tick, undo via 256-entry snapshots, serialize/hydrate), `solver` (constraint + enumeration fallback for No Guess), `metrics` (3BV). Vitest config moved to `vitest.config.mts` to silence ESM-in-CJS warning. 51 engine tests pass. First-reveal board generation uses `generatedFor` + generous-opening neighborhood; chord() returns null when not applicable (no phantom undo snapshots).
- **2026-09-13** Stage 3 UI shell done: `src/game/` Zustand glue (`useSettingsStore` persist `swm.settings.v1` + document data-* application; `useGameStore` wraps GameEngine with clock/board version counters, continue persistence `swm.run.v1` via serialize/hydrate, `unflag` engine method for tap-to-unflag, cols/rows tracking), `src/game/sound.ts` (Web Audio generative, zero assets) + haptics. `src/components/ui/` primitives (Button, IconButton, SegmentedControl, Switch, Sheet) + hand-drawn SVG icon set. `src/components/game/`: GameScreen (rAF clock, keyboard nav + shortcuts, visibility auto-pause, result detection), Hud (mines/status/timer + undo/restart), BoardGrid (ResizeObserver cell sizing, MIN_CELL floor so boards scroll instead of shrinking to untappable targets, mines-grid utility), Cell (per-cell state signature subscription, pointer gestures: tap reveal, tap-flagged unflag, long-press flag, double-tap chord, right/middle click), Overlays (Continue/Pause/Result), ModeSelect (modes disabled except Classic with "Soon" tag), SettingsSheet (theme/accent/font/scale/density/motion/feedback). Inline `inlineTokens.ts` restores saved tokens before first paint. 52 tests, typecheck + static export build green.
- **2026-09-13** SegmentDisplay skipped, swapped for compact tabular HUD per plan; "face" replaced by color-tinted restart icon (no emoji rule); question mark enabled in Classic cycle (game-logic questionAllowed).
- **2026-09-13** Stage 5 modes done. Engine: `BoardConfig` gained `noGuess`, `openAt: "click"|"center"`, `timeLimitMs`; `boardIsLogical` takes an optional startIndex and `generateNoGuessBoard` validates from the center opening; `GameEngine` gains generation branches — No Guess retry loop (attempt 0 uses the config seed, later candidates derive from `createRng(seed)`, fall back to last safe board) and Daily fixed boards (`openAt:"center"` via `generateNoGuessBoard`, same layout for every player regardless of first click) — plus `tick()` timeout loss (reason "Time's up.", elapsed clamped) and a `timeLimitMs` getter. `rng.ts` adds `dailySeed(dateISO, variant)` + `DAILY_GENERATOR_VERSION = "v1"`. Presets rebuilt data-driven: `PresetId` (`beginner/intermediate/expert/advanced/custom`), `PRESETS_BY_MODE` (No Guess beginner/advanced, Rush beginner 60s / intermediate 150s / expert 300s, Zen = Classic sizes, Daily 9x9/12), `getPreset(mode, id)`, `clampCustom`. `useGameStore.buildConfig` branches per mode (daily seed `swm|daily|{date}|daily|v1`, rush 3-state flag circles, undo gated by `modeDef.undoAllowed`), and SavedRun keeps `generationSeed` so a Daily board stays identical across midnight continues. UI: all 5 modes enabled in ModeSelect with per-mode difficulty panels (Daily shows today's date, Rush the countdown), HUD shows a red-tinted countdown for Rush and a Zen chip instead of a timer for Zen, keyboard hint line adapts per mode, ResultOverlay renders per-mode stats (Rush: Score/Time/Cleared with "Time beaten"/"Out of time" copy; Zen: Mines/Moves only). `mines-grid` gets `min-height: 0px` so grid tracks can shrink inside the flex board area. 56 engine tests, typecheck + static export build green.
- **2026-09-13** Stage 6 stats + persistence done. Engine gained a `flagsPlaced` counter (snapshot-backed, so undo cannot fake a no-flag run). New `src/game/stats.ts`: headless and pure. Per-mode-per-difficulty `ModeStats` (games/wins/losses, best time, average win time, best Rush score, cells revealed, mines found, flags placed, perfect games, current + best streaks) via `statsKey(mode, bucket)`; `recordRun(...)` is a pure reducer returning stats + achievements + daily history + recent games + `recordBeaten` + `unlocked` in one pass. Records are competitive-gated (`modeDef.competitive`); time records are skipped for Zen/No Guess and Rush scores are tracked instead of time for Rush. Eight real achievements: First Clear, Pure Logic (10 No Guess wins), No Flagger, Speed Demon (Expert < 2:00), Perfect Run, Daily Habit (3-day streak), Zen Mind (5 wins), Rush Hour (1500+). Daily history keeps one entry per calendar date with `dailyStreak()` (consecutive wins ending today or yesterday). Recent games capped at 20. Persistence is corrupt-safe: `loadPersistent`/`savePersistent` with per-validator checks and `loadAll` replaces only the corrupted slice (`swm.stats.v1`), never wiping unrelated data. `src/game/useStatsStore.ts` adds a tiny Zustand surface (`outcomeFor` builds a `RunOutcome` from the engine, `recordFinished` is content-addressed so StrictMode cannot double-count) plus `aggregateStat` for views. UI: header Trophy button opens the new `StatsSheet` (Overall + per-mode tabs, compact stat grid, per-mode breakdown, achievements list, recent games; empty modes show a placeholder instead of fake zeros) and Winner/Loser overlay now shows a "New personal best" pill and one-line achievement unlock. 87 tests (30 new), typecheck + static export build green. Keybinding remap and a global question-mark toggle were not added: controls stay as display hints and flag/undo behavior is mode-config-driven (one source of truth).
- **2026-09-13** Stage 7 custom primitives + polish done. New custom primitives in `src/components/ui/`: `ContextMenu` (store-backed layer, opens at pointer, clamped to viewport, Escape/backdrop closes), `CommandPalette` (Ctrl/Cmd+K filterable commands built from real store actions: new game, restart, pause/resume, per-mode start, records/settings/help, sound/haptics toggles with toasts; arrows + Enter + Escape), `Toasts` (transient bottom-center confirmations on command actions), `Tooltip` (hover/focus tooltips for the four header icon buttons), `Slider` (token-driven drag + arrow-key control) wired to a new real "Hold duration" setting (200-900ms). New `HelpSheet` modal (`?` key or header help icon): gesture and keyboard reference, mode-adaptive (undo line, question-mark note, mode description). Icons added: EyeIcon, FlagOffIcon, HelpIcon, CommandIcon, MouseIcon. Compliance edge: `game-logic.md` §10 mandates desktop right click = flag, so the cell right-click context menu was dropped and right-click flags (cycle) as specified; the custom context menu (per design.md §127) is wired to the board surface instead, offering New game, Restart, Pause/Resume, Commands, How to play, Settings. Motion/haptic/sound audit: reduced-motion (CSS kill-switch and `useReducedMotion`), global `:focus-visible` ring, theme-token scrollbars and board cursor verified. Bug found and fixed: `src/app/globals.css` was never imported, so dev and the static export emitted zero CSS; added `import "./globals.css"` to `src/app/layout.tsx` (export now ships a 52KB stylesheet). 87 tests, typecheck + build green. Deferred (need Arun): solver-backed tiered hint system (game-logic §30), pan/pinch zoom for large boards (§45), master/gameplay/UI volume split (§42), and custom-board first-click-safety/question-mark fields (§48). Post-commit QA round: fixed an unplayable start path (pre-first-click cells were inert divs — clicking or tapping the board did nothing; the trace is now a grid of live start buttons that generate the board), added a cell hover/press layer (fine-pointer hover lifts the cell with the pressed token, tactile press scale), a staggered reveal wave (chord reveals ripple 14ms apart), flag-plant/question/wrong-flag-shake microinteractions, nightly countdown pulse for Rush, spring-entrance overlay cards with a choreographed win/loss entrance (staggered title, count-up stats, popping record/achievement badges), and an app-wide reduced-motion guard (`MotionConfig reducedMotion="user"` + JS-animation fast path). typecheck, 87 tests, and static export build green.
- **2026-09-13** Readability + HUD pass: the pale light-theme secondaries were unreadable (--ink-muted #8e8e93 / #9ca3af / #87744d on paper/mist/sepia), so those were darkened to #6b6b71 / #5c6570 / #6b5b37 to pass ~4.5:1 on their surfaces; HUD gained a run-progress strip above the board showing Flags / Discovered / Left counts (token icons, tabular numerals, glass chip, live progress hairline that animates width as safe cells are uncovered); wordmark now uses the display face. typecheck, 87 tests, static export build green.
- **2026-09-13** Flag counter fix + layout/practice/onboarding pass. Root cause of "unflag does not decrease": the HUD strip was showing `flagsPlaced` (cumulative placements, needed by the No Flagger achievement/stats so undo cannot fake a clean run). Added a live `GameEngine.flagsOnBoard` getter (`countState("flagged")`) and pointed both HUD counters at it; engine mutations were NOT changed, preserving cumulative semantics. Regression tests cover live-vs-cumulative counts and unflag. HUD now fits the counts, clock, and mode button in one compact row on laptops (`MiniRunStats` inline chips shown lg+, stacked strip `lg:hidden` on mobile). Board centers on both axes inside its scroller (flex wrapper + `m-auto` grid, scroll-safe when the board overflows). New Practice mode (GraduationIcon): `train` flag on `ModeDefinition`, timer none / score zen / undo+hints+questions on, non-competitive; wired through `MODE_LIST`, `PRESETS_BY_MODE`, ModeSelect (icon, hint, start label), command palette, and Cell renders a faint mine/number hint (opacity 40) under hidden cells so new players learn to read the board. New first-run `OnboardingOverlay`: persisted `onboarded` flag in `swm.settings.v1`, spring card + stepped intro (tap reveal / hold flag / double-tap chord / practice pointer), Skip + progress dots, mounted via AnimatePresence. typecheck, 89 tests, static export build green, detector clean.


- **2026-09-13** Solver-backed teaching hint system (game-logic §30) done. New pure `src/engine/hints.ts`: `findHint(board, config)` returns a `HintResult` (action reveal/flag, actionCells, clueCells, areaCells, one-sentence explanation, level "deduce"/"guess") computed from the board's own dimensions and forced by the same constraints as No Guess (Level-1 number check first, then the full `solver.deduce`; no mine positions are ever leaked). Best-guess fallback picks the frontier cell touching the fewest revealed numbers ("No cell is forced yet..."). `GameEngine` gains `hintsUsed` (EngineState, serialized/hydrated, reset on restart, never in HistoryEntry so undo cannot game it), a `canHint` getter gated to playing, and `hint()` which increments the counter the moment help is requested. Store adds `hint` state + `askHint/applyHint/clearHint` (apply is math-safe: skips flagged, flags questioned via two cycles, reveals with flood/win-check). Stats: `RunOutcome.hintsUsed` disqualifies only the personal record in competitive modes (bestTime/bestScore skipped, `recordBeaten` false) while games/wins/streaks/achievements/daily/recent and time/score totals still count. UI: `LightbulbIcon` (hand-drawn), HUD hint button between Undo and Restart (amber, badge shows hints used, shown only when `modeDef.hintsAllowed`), `HintBanner` (spring card above the board, explanation + competitive disclaimer + "Show me"/"Flag it" + dismiss), per-cell `hintRole` tiering with `.hint-area/.hint-clue/.hint-action` (inset token-colored rings + pulse keyframe), keyboard `H`, command-palette entry, HelpSheet rows + one-paragraph explainer, footer shortcut line. Copy follows the no-em-dash rule. 10 new tests (hints finder: satisfied reveal / forced flag / guess / null / determinism; engine: gating, counting + undo/hydrate/restart, apply-safety; stats: hinted runs never set records but keep totals + achievements). typecheck, 99 tests, static export build green, detector clean.
- **2026-09-13** Pan + pinch zoom (game-logic §45) done. The board now sits in an explicitly sized wrapper (`boardW/H × zoom`) with the `.mines-grid` CSS-scaled from its top-left corner, so zooming grows the scrollable area instead of shrinking the page; panning stays native scroll plus a grab-to-pan drag on the board background (`.board-grab` / `.board-panning` cursors). Zoom in 1..3x comes from ctrl/trackpad wheel (native non-passive `wheel` listener with focal-point scroll correction) on desktop and from two-finger pinch on touch (pointer-distance based, focal midpoint, scroll re-anchored each frame). A shared `boardZoom.ts` singleton reports every pointer down/up inside the board; `useCellGestures` consults `pinchTapSuppressed()` so a second finger landing cancels reveals, long-press flags, and the lift that ends a pinch is never replayed as a tap (§47 gesture separation). Zoom resets to 1x on every new board, and `layout.tsx` viewport now locks `maximumScale: 1` / `userScalable: false` so page-level pinch cannot fight the board. `MIN_CELL` floor still guarantees touchable targets. typecheck, 99 tests, static export build green, detector clean.

- **2026-09-13** Custom-board toggles (game-logic §48) done. `CustomBoardSpec` gained `firstClickSafe` and `questionMarks` booleans with `CUSTOM_DEFAULTS` both `true`; `clampCustom` normalizes the new keys (`!== false`, so legacy saved runs without the flags fall back safe). `buildConfig` applies the custom values on the classic `custom` path only, keeping every other mode on its declared defaults (Rush stays question-free). ModeSelect's custom panel gained two `Switch` rows ("First-click safety" - the board opens safely around your first tap; "Question marks" - the flag cycle passes through a question state) alongside the existing Width/Height/Mines steppers, and the mines stepper still caps at 85% of cells with the engine clamping to the 5..60 board range (§48 validation). typecheck, 99 tests, static export build green, detector clean.
- **2026-09-13** Sound volume split (game-logic §42) done. `sound.ts` now runs a three-tier Web Audio gain chain — master → destination, with independent `gameplay` and `ui` buses feeding it — and every `playSound` call routes through `busFor(kind)` (board sounds vs chrome sounds). `applyVolumes()` reads `useSettings` on every context resume/creation, so toggles and sliders apply instantly with no extra plumbing. `useSettingsStore` gained `volumeMaster` (0.7), `volumeGameplay` (1), `volumeUi` (1) in the persisted `swm.settings.v1` bundle (missing keys fall back to defaults). SettingsSheet has a Sound switch row followed by three token-styled volume sliders (Master / Gameplay / UI) with percentage readouts; sliders use a quiet setter so dragging does not click back at you, while toggles still preview. typecheck, 99 tests, static export build green, detector clean.
- **2026-09-13** Cursor pass (partial): global pointer rules in `globals.css` — `button:not(:disabled)`, `[role="button"]`, `summary`, `select` get `cursor: pointer`; disabled controls `not-allowed`; range inputs pointer; `.cell-surface` explicitly `default`; live hidden cells `pointer`; chordable revealed number cells get `cursor-pointer`. Board pan cursors (grab/grabbing) still pending the §45 pan/zoom work.

## Notes

- Commit at every meaningful milestone, clear messages, never batch unrelated changes.
- Engine must be headless-testable; UI never contains game logic.
